version: 1

# UI entity spec for the debug UI specs browser.
# This is a "virtual" entity - data comes from /hit-ui-specs.json, not a database table.
entities:
  debug.uiSpec:
    meta:
      titleSingular: UI Spec
      titlePlural: UI Specs
      descriptionPlural: Public UI entities from /hit-ui-specs.json
      routes:
        root: /admin/debug
        list: /admin/debug/ui-specs
        detail: /admin/debug/ui-specs/{entityKey}
      breadcrumbs:
        - { label: Debug, href: /admin/debug }
        - { label: UI Specs, href: /admin/debug/ui-specs }
      actions:
        allowCreate: false

    # Schema-driven page security (used for EntityList/Detail/New/Edit).
    # Debug is admin-only; scope is still enforced via debug-core.*.read.scope.* actions.
    security:
      list:
        roles: []
        default_roles_allow: [admin]
        authz: { entity: debug, verb: read, show_pages: true }
      detail:
        roles: []
        default_roles_allow: [admin]
        authz: { entity: debug, verb: read, show_pages: true }
      new:
        roles: []
        default_roles_allow: [admin]
        authz: { entity: debug, verb: write, show_pages: true }
      edit:
        roles: []
        default_roles_allow: [admin]
        authz: { entity: debug, verb: write, show_pages: true }

    api:
      resource: ui-specs

    list:
      tableId: debug.uiSpecs
      uiStateVersion: 1
      pageSize: 50
      initialSort:
        sortBy: entityKey
        sortOrder: asc
      sortWhitelist:
        - entityKey
        - tableId
        - drizzleTable
        - listColumnCount
        - fieldCount
        - formSectionCount
        - relationCount
        - headerActionCount
        - detailExtrasCount
      initialColumnVisibility:
        entityKey: true
        tableId: true
        drizzleTable: true
        listColumnCount: true
        fieldCount: true
        formSectionCount: false
        relationCount: false
        headerActionCount: false
        detailExtrasCount: false
      initialSorting:
        - id: entityKey
          desc: false
      columns:
        - entityKey
        - tableId
        - drizzleTable
        - listColumnCount
        - fieldCount
        - formSectionCount
        - relationCount
        - headerActionCount
        - detailExtrasCount

    # Canonical fields map (virtual entity; all fields are derived from /hit-ui-specs.json).
    fields:
      entityKey: { type: text, label: Entity }
      tableId: { type: text, label: tableId }
      drizzleTable: { type: text, label: drizzleTable }
      listColumnCount: { type: number, label: "# list cols" }
      fieldCount: { type: number, label: "# fields" }
      formSectionCount: { type: number, label: "# form sections" }
      relationCount: { type: number, label: "# relations" }
      headerActionCount: { type: number, label: "# header actions" }
      detailExtrasCount: { type: number, label: "# detail extras" }

    detail:
      summaryFields:
        - entityKey
        - tableId
        - drizzleTable
        - fieldCount
        - formSectionCount
        - relationCount
        - headerActionCount
        - detailExtrasCount
      extras:
        - kind: uiSpecDetail
          platforms: [web]
